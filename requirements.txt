# 分店評價查詢系統 Python GUI 工具依賴

# 主要依賴
requests>=2.28.0       # HTTP 請求庫
tkinter                # GUI 框架 (Python 內建)

# 可選依賴（用於更好的用戶體驗）
Pillow>=9.0.0         # 圖像處理 (如果需要自定義圖標)

# 開發依賴
pytest>=7.0.0         # 測試框架
pytest-cov>=4.0.0     # 測試覆蓋率

# 注意事項:
# 1. tkinter 是 Python 內建模組，通常不需要額外安裝
# 2. 如果在某些 Linux 發行版中缺少 tkinter，可能需要安裝:
#    sudo apt-get install python3-tk (Ubuntu/Debian)
#    sudo yum install tkinter (CentOS/RHEL)
# 3. 此工具需要搭配 Node.js 後端服務器使用